// Single-user placeholder shared by ArticleFeedbackService/UserSourcePreferenceService and
// DigestBuilderService's recipient-personalization path until real multi-tenant user linking
// lands for those flows (Phase 5 for actions, Phase 10 for digest delivery — see the MVP3 plan).
export const DEFAULT_USER_ID = 'default_user';
